let firstName: string = 'Robert';
let lastName: string = 'Sharp';

//Create one function with two string parameters and return 
//a concatenated string of the parameterized values
function myName(firstName: string, lastName: string): string {
    return 'Hello' + firstName + ' ' + lastName;
}

console.log(myName);


